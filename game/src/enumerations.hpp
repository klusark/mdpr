#ifndef enumeration_hpp
#define enumeration_hpp

enum spriteTypes
{
	player,
	platform,
	bubble
};

enum keys
{
	keyDown,
	keyUp,
	keyLeft,
	keyRight,
	keyAction,
};

enum powerups
{
	gun,
};

#endif // ifndef enumeration_hpp